<h1>Projects Looking for Backers</h1>
<div class="links">
   <!-- link_to 'My Account', user_url(@user.id) -->
</div>

<h1><small><%= page_entries_info @projects %></small></h1>

<%= form_tag projects_path, method: :get, authentication: false, id: 'search-form' do %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search" %>
<% end %>

<div id="projects">
  <%= render @projects %>
</div>

<%= paginate @projects %>
